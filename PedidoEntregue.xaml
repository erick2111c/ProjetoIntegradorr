<Page x:Class="WpfApp1.PedidoEntregue"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfApp1"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="PedidoEntregue">

    <Viewbox Stretch="Uniform">
        <Grid Height="450" Width="800">

            <!-- Fundo -->
            <Grid.Background>
                <ImageBrush ImageSource="/pagamn.png" Stretch="Fill"/>
            </Grid.Background>

            <!-- Conteúdo que pode redimensionar -->


            <Rectangle Height="30"
                           VerticalAlignment="Top"
                           Width="800"
                           Fill="White"/>

            <Rectangle Width="347"
                           Height="98"
                           VerticalAlignment="Top"
                           Margin="212,316,241,0"
                           Fill="#FFF5F5F4"/>

            <Button Content="Ir para Login"
                Width="138"
                Height="38"
                HorizontalAlignment="Left"
                VerticalAlignment="Top"
                Margin="340,242,0,0"
                Background="#FFFF4109"
                Foreground="White"
                BorderBrush="#FFFF4109"
                FontSize="22" RenderTransformOrigin="0.488,0.93" Click="Button_Click_1"/>

        </Grid>
    </Viewbox>
</Page>